# Optional CoinGecko config
COINGECKO_PLAN=demo            # demo | pro
COINGECKO_API_KEY=
COINGECKO_MODE=off             # on | off | sample
COINGECKO_SAMPLE=0.25          # 0..1 when mode=sample

# Fetch cadence / behavior
ALWAYS_TICK=1

# Universe filtering
CVI_INCLUDE=
CVI_EXCLUDE=

# Email notifications (notifyEmail.js)
EMAIL_USER=
EMAIL_PASS=
TO_EMAIL=
NOTIFY_ON=strong               # strong | non-hold | all
NOTIFY_MIN_STRENGTH=0.5
EMA_CROSS_THRESHOLD=0

# Strategy sizing
RISK_BUDGET_USD=100
HORIZON_DAYS=1
DEFAULT_LEVERAGE=1
